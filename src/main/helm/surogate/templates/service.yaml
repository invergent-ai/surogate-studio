apiVersion: v1
kind: Service
metadata:
  name: {{ include "surogate.fullname" . }}
  labels:
    {{- include "surogate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "surogate.selectorLabels" . | nindent 4 }}

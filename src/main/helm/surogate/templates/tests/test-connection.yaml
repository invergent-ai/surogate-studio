apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "surogate.fullname" . }}-test-connection"
  labels:
    {{- include "surogate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "surogate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
